# xp-embed

```python
# DROP entire database (before recreating everything)
python reset.py database
# Drop table TABLENAME (before recreating everything)
python reset.py table TABLENAME
# Drop nothing, just create everything missing
python reset.py
```

